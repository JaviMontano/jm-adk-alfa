# QA Findings

Date: 2026-05-28
Branch: `qa/adversarial-exploratory-hardening`

| id | severity | type | finding | evidence | reproduction | user impact | agent risk | improvement | no-regression test | decision |
|---|---|---|---|---|---|---|---|---|---|---|
| QA-001 | P1 | skill | Scaffold accepted path-like skill names by normalizing them instead of rejecting them. | Pre-fix `slugify()` converted arbitrary names to slugs without path intent validation; fixed in `scripts/scaffold-skill.py`. | `python3 scripts/scaffold-skill.py --name ../evil --dry-run` | User or agent can believe a path was honored while a different slug is generated. | Agent may route generated files to an unintended skill identity. | Reject absolute paths, `/`, `\`, `..`, and dot-prefixed names before slugification. | `scaffold_rejects_path_traversal` | fix now |
| QA-002 | P1 | skill | Existing skill slugs could produce a successful dry-run or apply with zero writes unless `--force` was understood. | Pre-fix `write_scaffold()` skipped existing files and returned success; fixed in `scripts/scaffold-skill.py`. | `python3 scripts/scaffold-skill.py --name bmad-method --description "Duplicate" --dry-run` | A vibe coder thinks a skill was created or updated when nothing happened. | Agent reports false completion after a no-op. | Fail duplicate skill roots unless `--force` is explicit or `--all-existing` is used. | `scaffold_rejects_duplicate_without_force` | fix now |
| QA-003 | P1 | skill | Scaffold allowed unknown tool names. | Pre-fix scaffold did not validate `--allowed-tools`; validator had the canonical set. | `python3 scripts/scaffold-skill.py --name qa-bad-tool --allowed-tools Read,Destroy --dry-run` | Skill authors can publish unusable tool contracts. | Agent tries to call unavailable tools or misstates capability. | Validate allowed tools at scaffold time using the same contract as the validator. | `scaffold_rejects_unknown_tool` | fix now |
| QA-004 | P1 | testing | Strict skill validation treated unknown allowed tools as warnings, so CI could pass invalid skill contracts. | Pre-fix `scripts/validate-skills.py` appended unknown tools to `warnings`; fixed to error in strict mode. | Temporary fixture with `allowed-tools: Destroy`; run `python3 scripts/validate-skills.py --strict`. | Broken skills reach users despite green validation. | Agent routing depends on a false tool surface. | Promote unknown tools to errors in strict mode. | `validator_rejects_unknown_tool_strict` | fix now |
| QA-005 | P1 | workspace | Boundary checks missed tracked `.codex/` local state. | `.gitignore` ignored `.codex/`, but `scripts/check-repo-boundaries.sh` did not fail if `.codex/` was already tracked; fixed. | Temporary repo with staged `.codex/config.toml`; run boundary checker. | Local agent config can leak or create non-portable behavior. | Agents inherit local-only runner state from repo. | Add `.codex/` tracked-state failure. | `boundaries_detect_tracked_codex` | fix now |
| QA-006 | P2 | docs | Several docs and metadata surfaces referenced stale repo names, counts, or validation commands. | `CONTRIBUTING.md` used `JaviMontano/jm-agentic-development-kit`, `docs/advanced/custom-skills.md` used `.agent/scripts/*`, and `.claude-plugin/marketplace.json` advertised 512 skills before this branch. | Read contribution, custom skill, catalog, and marketplace docs. | Contributors follow commands that do not match the target repo. | Agent repeats stale setup instructions in future tasks. | Update docs and metadata to `jm-adk-alfa`, current counts, and current root scripts. | Docs review plus baseline validation commands. | fix now |
| QA-007 | P2 | testing | Adversarial guardrails were not consolidated in a repeatable QA command or CI step. | Existing CI ran validators and a smoke dry-run, but not failure-path fixtures. | Inspect `.github/workflows/validate.yml`. | Regressions appear only during real user failures. | Agent hardening claims become QA theater. | Add `scripts/qa/run-adversarial-tests.py` and CI step. | `python3 scripts/qa/run-adversarial-tests.py` | fix now |
| QA-008 | P2 | UX | Failure recovery was fragmented across scripts and docs. | Safe sync docs covered Git failures, but no broad troubleshooting guide existed. | New user hits invalid JSON, duplicate skill, dirty tree, or local config leak. | User does not know what to paste back to an agent. | Agent may infer instead of requiring evidence. | Add troubleshooting doc with failure report template and halt-and-report guidance. | Docs review; adversarial tests provide examples. | fix now |
| QA-009 | P2 | maintainability | Output artifacts such as CSV/HTML/Excel are not protected by repo-wide contract fixtures. | Scaffold has output templates, but no generic generated-output validation fixtures were found. | Search docs/scripts for output contract tests. | Deliverables can look plausible without meeting acceptance criteria. | Agent optimizes for file count instead of quality gates. | Backlog output-contract suite with fixtures and acceptance checks. | Not implemented in this branch. | backlog |
| QA-010 | P3 | portability | Windows support is not proven. | Scripts and CI target Bash/Python on Ubuntu/macOS-like paths; no Windows workflow exists. | Review `.github/workflows/validate.yml` and shell scripts. | Windows users may assume support that is not tested. | Agent may overstate portability. | Document macOS/Linux primary support or add Windows CI later. | Not implemented in this branch. | backlog |
