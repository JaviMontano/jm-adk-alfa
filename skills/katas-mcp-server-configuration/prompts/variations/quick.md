<!--
generated-by: scripts/scaffold-skill.py
generated-for: katas-mcp-server-configuration
generated-on: 2026-05-29
overwrite-policy: missing-only unless --force
-->

# Kata 22 · Quick Variation

Usa cuando el server y su scope son claros (ej. "agrega el server de GitHub al `.mcp.json` del repo").

Devuelve directamente el bloque `mcpServers` con la credencial via `${ENV_VAR}`, una nota de las env-vars requeridas y el estado de validacion. Sin discovery extenso.
