# Output Contract Enforcer Scripts

- `validate_output_contract.py`: validates a generated artifact against a JSON contract.
- `check.sh`: runs deterministic fixture checks for pass, missing section, missing evidence tags, missing JSON fields, and naming violations.
