# Task Engine Scripts

Offline validators for deterministic DSVSR packets.

Run:

```bash
bash skills/task-engine/scripts/check.sh
```

The check validates full DSVSR, missing context, low confidence, false-positive routing, and verification failure fixtures.
