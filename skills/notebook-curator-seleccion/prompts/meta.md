# Meta Prompt

Before claiming a notebook is ready, verify every canonical slot, one source per slot, evidence details, offline retrieval mode, and curation action safety. Block summaries when source content evidence is missing.
