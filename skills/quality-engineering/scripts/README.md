# Quality Engineering Scripts

`validate_quality_engineering.py` validates structured JSON quality engineering reports against the offline assets.

`check.sh` runs valid fixtures and deterministic invalid mutation cases.
