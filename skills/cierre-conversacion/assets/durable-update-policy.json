{
  "schema": 1,
  "skill": "cierre-conversacion",
  "allowed_targets": [
    "tasklog",
    "changelog",
    "memory",
    "review_doc",
    "ledger",
    "none"
  ],
  "actions": ["append", "propose", "skip"],
  "authority": ["confirmed", "pending", "not-applicable"],
  "rules": [
    "append requires confirmed authority",
    "propose is required when authority is pending",
    "skip is required when no durable update is needed",
    "review_doc and ledger updates belong to the active skill only"
  ]
}
