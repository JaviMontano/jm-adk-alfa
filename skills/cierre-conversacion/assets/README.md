# Cierre Conversacion Assets

These assets define the deterministic closeout contract used by `SKILL.md`, evals, templates, and the offline validator.

The skill is report-only by default. Any tasklog, changelog, memory, or repo documentation update must be listed in the durable update plan and must declare whether authority is confirmed, pending, or not applicable.
