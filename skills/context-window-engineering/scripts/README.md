# Context Window Engineering Scripts

- `compile-context-window.py` validates a JSON context assembly spec and renders a deterministic Markdown report. [CÓDIGO]
- `check.sh` runs positive fixtures, negative fixtures, syntax checks, and generated-fragment checks. [CÓDIGO]
