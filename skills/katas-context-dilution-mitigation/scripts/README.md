# Katas Context Dilution Mitigation Scripts

`validate_context_dilution_report.py` validates deterministic JSON reports for edge placement and compaction decisions. It checks local fixtures only and uses no network, time, random values, or repository mutation.

Run:

```bash
bash skills/katas-context-dilution-mitigation/scripts/check.sh
```
