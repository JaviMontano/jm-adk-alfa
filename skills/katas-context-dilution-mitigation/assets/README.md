# Katas Context Dilution Mitigation Assets

Deterministic contracts for Kata context dilution mitigation. These assets define edge placement, the 50-60 percent compaction threshold, preservation requirements, and the JSON report contract used by the local validator.

Use `manifest.json` as the index and `report-contract.json` with `scripts/validate_context_dilution_report.py` for fixture validation.
