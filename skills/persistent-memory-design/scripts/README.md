# Persistent Memory Design Scripts

`validate_persistent_memory_report.py` validates deterministic JSON reports for persistent scratchpad designs. It checks local fixtures only and uses no network, time, random values, or repository mutation.

Run:

```bash
bash skills/persistent-memory-design/scripts/check.sh
```
