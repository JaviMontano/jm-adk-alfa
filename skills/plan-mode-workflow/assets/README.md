# Plan Mode Workflow Assets

These assets define the deterministic contract for a two-mode workflow: Plan Mode read-only, signed plan approval, PreToolUse hook enforcement, re-sign on plan change, and guarded transition to Execute Mode.
