# Provenance Engineering Scripts

`validate_provenance_engineering.py` validates JSON reports against typed provenance policies in `../assets`.

Run:

```bash
bash skills/provenance-engineering/scripts/check.sh
```

The check executes valid fixtures and confirms invalid mutations fail deterministically.
