# generate-qa-report Scripts

`check.sh` validates deterministic QA report fixtures.

The validator reads local JSON assets and fixtures only. It does not run QA tools, inspect the network, or use wall-clock time.
