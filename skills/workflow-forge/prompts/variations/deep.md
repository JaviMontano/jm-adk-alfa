---
name: workflow-forge-deep
type: variation
variant: deep
---

# Workflow Forge - Deep Mode

Load `SKILL.md`, `knowledge/body-of-knowledge.md`,
`references/domain-knowledge.md`, and all files under `assets/`. If a JSON spec
exists, run the deterministic compiler and include validation evidence. Inspect
existing commands/workflows before writing to avoid duplicate command names.
