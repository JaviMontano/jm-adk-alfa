<!--
generated-by: scripts/scaffold-skill.py
generated-for: server-middleware
generated-on: 2026-05-28
overwrite-policy: missing-only unless --force
-->

# Server Middleware

Auth verification (verifyIdToken), rate limiting, CORS config, request logging, input sanitization middleware. [EXPLICIT]

## Triggers

- server-middleware

## Allowed Tools

- Read
- Grep
- Glob
- Bash

## Quick Use

Use this skill when the request clearly matches the triggers and requires the `server-middleware` capability.

## Output Format

Markdown with summary, evidence, result, validation, and risks.
