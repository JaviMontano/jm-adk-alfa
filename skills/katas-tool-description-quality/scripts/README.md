# Katas Tool Description Quality Scripts

`validate_tool_description_quality.py` validates structured JSON reports for tool description quality.

`check.sh` runs valid fixtures and deterministic invalid mutation cases.
