# Katas Session Resume Fork Scripts

`validate_kata_session_report.py` validates deterministic JSON reports for Kata 25 decisions. It checks local fixtures only and uses no network, time, random values, or repository mutation.

Run:

```bash
bash skills/katas-session-resume-fork/scripts/check.sh
```
