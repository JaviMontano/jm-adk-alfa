# Katas Session Resume Fork Assets

Deterministic contracts for Kata 25 session continuation decisions. These assets define the offline decision matrix, stale-result signals, fork isolation, typed summary requirements, and JSON report shape used by the local validator.

Use `manifest.json` as the index and `report-contract.json` with `scripts/validate_kata_session_report.py` for fixture validation.
