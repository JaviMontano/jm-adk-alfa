# Evaluation Confidence Design Scripts

- `compile-evaluation-confidence.py` validates a JSON evaluation confidence spec and renders a deterministic Markdown report. [CÓDIGO]
- `check.sh` runs positive fixtures, negative fixtures, syntax checks, and generated-fragment checks. [CÓDIGO]
