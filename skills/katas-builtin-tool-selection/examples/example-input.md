<!--
generated-by: scripts/scaffold-skill.py
generated-for: katas-builtin-tool-selection
generated-on: 2026-05-29
overwrite-policy: missing-only unless --force
-->

# Example Input

En un repositorio Python, hay que cambiar el umbral hardcodeado de la función `processRefund`: la condición `if amount > 1000:` debe pasar a usar la constante `MAX_REFUND`. No sé en qué archivo está la función y el repo tiene cientos de archivos. Quiero la modificación aplicada sin cargar todo el repo en contexto.
