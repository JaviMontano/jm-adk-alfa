# Safe Scripting And Bash Scripts

`validate_safe_scripting_and_bash.py` validates JSON script-safety reports against the assets in `../assets`.

Run:

```bash
bash skills/safe-scripting-and-bash/scripts/check.sh
```

The check executes valid fixtures and confirms invalid mutations fail deterministically.
