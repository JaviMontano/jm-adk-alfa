# Assumption Log Scripts

Deterministic local checks for assumption log JSON reports.

- `validate_assumption_log.py` validates IDs, statuses, evidence tags, counts, warnings, contradiction links, decision links, and validation queue coverage.
- `check.sh` runs positive and negative fixtures without network, current time, random values, or provider calls.
