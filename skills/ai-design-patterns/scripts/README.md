# AI Design Patterns Scripts

Run:

```bash
bash skills/ai-design-patterns/scripts/check.sh
```

The check validates deterministic AI design pattern selection report JSON packets
against the local assets contract, closed pattern catalog, dependency policy,
anti-pattern policy, tactic policy, and roadmap policy.
