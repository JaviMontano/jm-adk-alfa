# Brand HTML Scripts

`check.sh` validates deterministic HTML fixtures with
`validate_brand_html.py`.

The validator checks token variables, semantic landmarks, responsive CSS,
unresolved placeholders, base64 images, external JavaScript, unapproved remote
assets, off-token hex usage, and simple contrast pairs.
