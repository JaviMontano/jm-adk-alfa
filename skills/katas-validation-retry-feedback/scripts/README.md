# Scripts — katas-validation-retry-feedback

`validate_validation_retry_feedback.py` validates a JSON retry report against the deterministic assets for this skill.

`check.sh` runs the validator against valid fixtures and invalid mutations. It is offline, deterministic, and does not require network access.
