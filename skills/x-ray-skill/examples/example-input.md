# Example Input

Audit `skills/workspace-governance` for certification readiness. Produce the full X-Ray report with rubric scores, 13 gate results, top issues, component classification, and recommended next step. Use the deterministic compiler first, then explain any limits of structural scoring.
