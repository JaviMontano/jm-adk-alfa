# Brand XLSX Assets

These assets make `brand-xlsx` deterministic. They define activation routing,
the XLSX workbook contract, fallback brand tokens, style-token mapping,
evidence requirements, and fixture-backed validation.

Run `bash skills/brand-xlsx/scripts/check.sh` after changing assets, fixtures,
templates, or validator logic.
