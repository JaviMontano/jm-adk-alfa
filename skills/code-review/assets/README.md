# Code Review Assets

These assets make `code-review` deterministic: activation is bounded, the
severity/category taxonomy is fixed, evidence tags are constrained, report
fields are machine-checkable, and source boundaries prevent write-side effects.

Run `bash skills/code-review/scripts/check.sh` after changing any asset,
fixture, template, or validator.
