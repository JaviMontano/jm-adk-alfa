# AI Pipeline Architecture Scripts

Run:

```bash
bash skills/ai-pipeline-architecture/scripts/check.sh
```

The check validates deterministic AI pipeline architecture report JSON fixtures
against local stage, store, registry, CI/CD, and requirements policies.
