# Example Input

Retomar el despliegue de skills en `JaviMontano/jm-adk-alfa`.

Antes de aplicar otro patch:

- confirma en que branch estoy;
- confirma si `origin/main` local apunta al ultimo squash mergeado;
- revisa si hay cambios dirty;
- compara `docs/audits/skill-review-ledger.csv` contra `skills/`;
- detecta review docs o skills con scripts que siguen `pending`;
- dime si puedo abrir otro PR granular sin mezclar staging.
