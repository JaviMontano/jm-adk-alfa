# AI Safety Scripts

Run:

```bash
bash skills/ai-safety/scripts/check.sh
```

The check validates deterministic AI safety report JSON fixtures against local
risk, control, jailbreak, evaluation, and escalation policies.
