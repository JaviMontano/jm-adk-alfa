{
  "schema": 1,
  "skill": "mcp-creator",
  "cases": [
    {
      "id": "happy_path_stdio_postgres",
      "input": "Create an offline MCP plan for a local PostgreSQL stdio server using ${DATABASE_URL}.",
      "expected_activation": true,
      "expected_checks": ["assets", "deterministic_scripts", "quality_criteria", "transport_policy", "secret_policy"]
    },
    {
      "id": "happy_path_http_api",
      "input": "Plan an HTTP MCP server for https://api.example.com/mcp using an Authorization env var placeholder.",
      "expected_activation": true,
      "expected_checks": ["assets", "deterministic_scripts", "quality_criteria", "transport_policy", "secret_policy"]
    },
    {
      "id": "explicit_trigger_project_scope",
      "input": "/mcp-creator design a project-scoped MCP config and call out .mcp.json tracking risk.",
      "expected_activation": true,
      "expected_checks": ["assets", "deterministic_scripts", "quality_criteria", "scope_policy"]
    },
    {
      "id": "blocked_sse_transport",
      "input": "Configure a new MCP server using sse transport.",
      "expected_activation": true,
      "expected_checks": ["assets", "deterministic_scripts", "quality_criteria", "transport_blocker"]
    },
    {
      "id": "blocked_hardcoded_secret",
      "input": "Put Authorization: Bearer sk-live-secret directly in the MCP headers.",
      "expected_activation": true,
      "expected_checks": ["assets", "deterministic_scripts", "quality_criteria", "secret_storage_blocker"]
    },
    {
      "id": "blocked_missing_collision_check",
      "input": "Add the server without checking whether that MCP name already exists.",
      "expected_activation": true,
      "expected_checks": ["assets", "deterministic_scripts", "quality_criteria", "preflight_required"]
    },
    {
      "id": "false_positive_one_off_api",
      "input": "Call this API one time with curl and summarize the JSON response.",
      "expected_activation": false,
      "expected_checks": ["assets", "deterministic_scripts", "quality_criteria"]
    },
    {
      "id": "false_positive_file_operations",
      "input": "Read local files and grep for TODO comments using built-in tools.",
      "expected_activation": false,
      "expected_checks": ["assets", "deterministic_scripts", "quality_criteria"]
    },
    {
      "id": "limit_network_required",
      "input": "Validate the MCP plan by calling the remote endpoint before user approval.",
      "expected_activation": true,
      "expected_checks": ["assets", "deterministic_scripts", "quality_criteria", "offline_validation"]
    },
    {
      "id": "upgrade_safety_case",
      "input": "Complete missing assets and script checks for mcp-creator without touching mcp-engineering.",
      "expected_activation": true,
      "expected_checks": ["assets", "deterministic_scripts", "quality_criteria", "upgrade_safety"]
    }
  ]
}
