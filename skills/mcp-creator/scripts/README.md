# MCP Creator Scripts

Run the offline fixture check:

```bash
bash skills/mcp-creator/scripts/check.sh
```

Validate a specific MCP configuration plan:

```bash
python3 skills/mcp-creator/scripts/validate_mcp_config_plan.py path/to/plan.json
```
