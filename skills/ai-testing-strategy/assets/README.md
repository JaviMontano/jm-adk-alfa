# AI Testing Strategy Assets

These assets define the deterministic report contract for `ai-testing-strategy`. They are consumed by `SKILL.md`, templates, evals, and `scripts/validate_ai_testing_strategy.py`.
