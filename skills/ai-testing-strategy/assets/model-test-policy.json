{
  "schema": 1,
  "required_categories": ["accuracy", "adversarial", "drift", "counterfactual", "regression"],
  "required_fields": ["category", "oracle", "threshold", "automation_tier", "evidence_ids"]
}
