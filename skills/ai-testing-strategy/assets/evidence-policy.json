{
  "schema": 1,
  "allowed_tags": ["[EXPLICIT]", "[INFERRED]", "[OPEN]"],
  "required_fields": ["id", "tag", "source", "summary"],
  "rule": "Every test category, integration decision, gate, and monitoring item must reference evidence or an explicit assumption."
}
