# AI Testing Strategy Scripts

`validate_ai_testing_strategy.py` validates JSON testing strategy reports against the local assets. `check.sh` runs deterministic positive and negative fixtures without network, wall-clock, or random inputs.
