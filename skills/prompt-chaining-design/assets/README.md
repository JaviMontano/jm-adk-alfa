# Prompt Chaining Design Assets

These assets define the deterministic map-reduce style prompt chaining contract: single-pass gate, atomic unit, local pass schema, transition schema, integration over summaries, typed per-unit errors and Guardian decision.
