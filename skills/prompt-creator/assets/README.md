# Assets

Reusable deterministic contracts for `prompt-creator`.

- `prompt-contract-checklist.md`: final gate for generated prompt artifacts.
- `prompt-type-matrix.json`: canonical prompt type matrix, required sections, redirects, and type-specific checks.

