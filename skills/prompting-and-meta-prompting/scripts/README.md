# Prompting And Meta Prompting Scripts

`validate_prompting_and_meta_prompting.py` validates JSON prompt-system reports against the assets in `../assets`.

Run:

```bash
bash skills/prompting-and-meta-prompting/scripts/check.sh
```

The check executes valid fixtures and confirms invalid mutations fail deterministically.
