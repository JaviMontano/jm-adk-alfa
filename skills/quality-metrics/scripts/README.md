# Quality Metrics Scripts

`validate_quality_metrics.py` validates structured JSON quality metrics reports against the offline assets.

`check.sh` runs valid fixtures and deterministic invalid mutation cases.
