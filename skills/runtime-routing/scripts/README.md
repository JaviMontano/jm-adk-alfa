# Runtime Routing Scripts

`validate_runtime_routing.py` validates JSON route reports against the assets in `../assets`.

Run:

```bash
bash skills/runtime-routing/scripts/check.sh
```

The check executes valid fixtures and confirms invalid mutations fail deterministically.
