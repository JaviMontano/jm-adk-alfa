{
  "schema": "jm-labs.workspace-setup.plan.v1",
  "skill": "workspace-setup",
  "mode": "dry-run",
  "target_file": ".jm-adk.local.json",
  "existing_profile": {
    "detected": false,
    "overwrite": false,
    "force": false
  },
  "profile": {
    "goal": "harden one skill at a time with PR-based evidence",
    "runtime": "Codex",
    "autonomy": "implement after local preflight is clean",
    "workspace_area": "workspace/",
    "output_format": "markdown"
  },
  "commands": {
    "allowed": ["git status --short --branch", "python3 -B scripts/validate-skills.py --strict"],
    "prohibited": ["git reset --hard", "rm -rf"],
    "escalation_required": ["network", "destructive"]
  },
  "privacy": {
    "local_only": true,
    "stores_secrets": false,
    "secret_scan_performed": true,
    "redactions": ["tokens", "passwords", "email_addresses"]
  },
  "write_policy": {
    "dry_run_default": true,
    "apply_requires_explicit_flag": true,
    "overwrite_requires_force": true,
    "gitignored": true
  },
  "evidence": [
    {
      "claim": "Dry-run setup does not write the local profile file.",
      "evidence_tag": "[DOC]",
      "source": "workspace setup contract"
    },
    {
      "claim": "Local-only privacy forbids secret storage.",
      "evidence_tag": "[CONFIG]",
      "source": "privacy policy"
    }
  ],
  "validation": {
    "status": "pass",
    "offline": true,
    "network_required": false,
    "deterministic": true,
    "checks": [
      "assets",
      "deterministic_scripts",
      "quality_criteria",
      "runtime_preferences",
      "command_policy",
      "privacy_policy",
      "write_policy",
      "evidence_required"
    ]
  }
}
