# Workspace Setup Scripts

Run the offline fixture check:

```bash
bash skills/workspace-setup/scripts/check.sh
```

Validate a specific setup plan:

```bash
python3 skills/workspace-setup/scripts/validate_workspace_setup_plan.py path/to/plan.json
```
