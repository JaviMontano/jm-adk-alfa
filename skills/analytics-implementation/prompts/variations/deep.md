---
name: analytics-implementation-deep
type: variation
variant: deep
---
# Analytics Implementation — Deep Analysis

Full depth execution. Load all `references/` files from canonical. Run L3 progressive loading.
Apply all Validation Gate criteria strictly.

Required additions:
- Inventory GA4 properties, Firebase apps, data streams, destinations, consent constraints, and owners.
- Map every event to parameters, conversion status, platform, validation method, and export path.
- Define BigQuery dataset, retention, partitioning, PII handling, and dashboard data source.
- Use `assets/` as the checklist and `scripts/check.sh` fixtures as the offline oracle pattern.
