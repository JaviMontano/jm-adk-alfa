{
  "schema": 1,
  "required_fields_when_used": ["name", "scope", "used_by"],
  "blocked_value_patterns": ["token_", "ghp_", "gho_", "ghs_", "npm_", "-----BEGIN"],
  "rules": [
    "Workflow plans may list secret names but must not include secret values.",
    "Secrets should be scoped to the job or environment that uses them.",
    "Deploy secrets require protected environment scope when available."
  ]
}
