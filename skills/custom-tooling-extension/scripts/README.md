# Custom Tooling Extension Scripts

- `compile-custom-tooling.py` validates a JSON extension spec and renders a deterministic implementation plan. [CÓDIGO]
- `check.sh` runs positive fixtures, negative fixtures, syntax checks, and generated-fragment checks. [CÓDIGO]
