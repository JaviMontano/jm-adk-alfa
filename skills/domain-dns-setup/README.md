<!--
generated-by: scripts/scaffold-skill.py
generated-for: domain-dns-setup
generated-on: 2026-05-28
overwrite-policy: missing-only unless --force
-->

# Domain Dns Setup

DNS configuration for Firebase Hosting (A/CNAME), email (MX), verification (TXT), and SSL certificate management

## Triggers

- domain-dns-setup

## Allowed Tools

- Read
- Grep
- Glob
- Bash

## Quick Use

Use this skill when the request clearly matches the triggers and requires the `domain-dns-setup` capability.

## Output Format

Markdown with summary, evidence, result, validation, and risks.
