# Input Analysis Assets

Deterministic contracts for parsing project inputs. These assets define allowed evidence tags, finding taxonomy, completeness scoring, assumption warning behavior, and the JSON report shape used by the local validator.

Use `manifest.json` as the index and `report-contract.json` with `scripts/validate_input_analysis_report.py` for fixture validation.
