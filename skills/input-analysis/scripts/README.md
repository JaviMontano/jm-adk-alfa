# Input Analysis Scripts

`validate_input_analysis_report.py` validates deterministic JSON reports for input analysis. It checks local fixtures only and uses no network, time, random values, or repository mutation.

Run:

```bash
bash skills/input-analysis/scripts/check.sh
```
