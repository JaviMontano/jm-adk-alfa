# ideate-component Scripts

`check.sh` runs the offline concept-card validator against deterministic fixtures.

The validator reads only files inside `skills/ideate-component/assets/` and the supplied fixture path. It does not use network access, wall-clock time, random values, or repository mutation.
