# Knowledge Management Scripts

`check.sh` runs deterministic offline validation for fixture reports. It accepts
valid knowledge-management reports and rejects reports with missing evidence,
missing retrieval terms, stale items without actions, invalid dates, or network
source dependencies.
