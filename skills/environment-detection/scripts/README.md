# Environment Detection Scripts

Run:

```bash
bash skills/environment-detection/scripts/check.sh
```

The check validates deterministic JSON environment detection reports. Valid fixtures must pass; invalid fixtures must fail for the expected reasons.
