# AI Architecture Audit Scripts

Run:

```bash
bash skills/ai-architecture-audit/scripts/check.sh
```

The check validates deterministic AI Architecture Audit JSON packets. Valid fixtures must pass; invalid fixtures must fail for expected contract violations.
