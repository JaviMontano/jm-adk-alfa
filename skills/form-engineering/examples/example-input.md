<!--
generated-by: scripts/scaffold-skill.py
generated-for: form-engineering
generated-on: 2026-05-28
overwrite-policy: missing-only unless --force
-->

# Example Input

Design the implementation contract for an enterprise intake wizard with:

- Step 1: work email.
- Step 2: company size and PDF security brief upload.
- Client validation and server validation must use the same rule names.
- Uploads must show progress, support retry, and store files under a private prospect boundary.
- Submission must be optimistic, idempotent, and must preserve the draft on network failure.
- Errors must be accessible through field-level messages and a top summary.

Run the deterministic contract compiler if the requirements can be expressed as JSON.
