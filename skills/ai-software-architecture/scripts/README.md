# AI Software Architecture Scripts

`validate_ai_architecture_report.py` validates JSON architecture reports against the local assets. `check.sh` runs deterministic positive and negative fixtures without network, wall-clock, or random inputs.
