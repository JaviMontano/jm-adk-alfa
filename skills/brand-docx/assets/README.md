# Brand DOCX Assets

These assets make `brand-docx` deterministic. They define activation routing,
the DOCX package contract, fallback brand tokens, style-token mapping, evidence
requirements, and fixture-backed validation.

Run `bash skills/brand-docx/scripts/check.sh` after changing assets, fixtures,
templates, or validator logic.
