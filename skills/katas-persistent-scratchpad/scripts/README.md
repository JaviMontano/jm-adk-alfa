# Katas Persistent Scratchpad Scripts

`validate_kata_scratchpad_report.py` validates deterministic JSON reports for persistent scratchpads. It checks local fixtures only and uses no network, time, random values, or repository mutation.

Run:

```bash
bash skills/katas-persistent-scratchpad/scripts/check.sh
```
