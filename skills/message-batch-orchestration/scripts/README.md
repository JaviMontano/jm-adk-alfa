# Scripts — message-batch-orchestration

`validate_message_batch_orchestration.py` validates a JSON batch orchestration report against the deterministic assets for this skill.

`check.sh` runs the validator against valid fixtures and invalid mutations. It is offline, deterministic, and does not require network access.
