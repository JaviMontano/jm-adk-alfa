# Deep Variation

Handle a high-risk cleanup or sync:

- Inventory sibling clones and worktrees in addition to the active repo.
- Preserve tracked work as patch artifacts and local-only files as archive or manifest-backed moves.
- Exclude private paths from publishable artifacts.
- Validate fixtures and the live preservation report.
- Block mutation until every failing invariant is resolved or documented as an explicit blocker.
