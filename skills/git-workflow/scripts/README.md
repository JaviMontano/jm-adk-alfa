# git-workflow Scripts

`check.sh` validates deterministic Git workflow-plan fixtures.

The validator reads only local JSON fixtures and assets. It does not run Git operations, call GitHub, mutate branches, or depend on wall-clock time.
