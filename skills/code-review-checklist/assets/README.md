# Code Review Checklist Assets

These assets make the checklist deterministic. They define activation rules,
fixed checklist IDs, blocking gates, evidence requirements, report structure,
and read-only source boundaries.

Run `bash skills/code-review-checklist/scripts/check.sh` after changing assets,
fixtures, templates, or validator logic.
