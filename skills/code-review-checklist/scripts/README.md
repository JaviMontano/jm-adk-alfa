# Code Review Checklist Scripts

`check.sh` validates deterministic checklist report fixtures using
`validate_code_review_checklist_report.py`.

The validator checks required sections, score fields, checklist IDs, status
values, source evidence, finding fields, evidence tags, decision rules, positive
evidence requirements, and invalid approval with blocking failures.
