# Assets

Policy and contract files for deterministic reference-network packets. The validator uses these concepts offline; no live social network, mailbox, calendar, or enrichment service is required.
