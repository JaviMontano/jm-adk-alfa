{
  "schema": 1,
  "allowed_tags": ["product_requirement", "existing_event", "tracking_destination", "privacy_constraint", "implementation_context", "validation", "assumption"],
  "required_fields": ["id", "tag", "source", "summary"]
}
