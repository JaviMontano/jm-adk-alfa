# Session Manager Assets

| Asset | Purpose |
|---|---|
| `state-contract.json` | Required JSON report keys, context statuses, decisions, and evidence tags. |
| `stage-policy.json` | Linear stage order, artifact evidence, progress rules, and skip prevention. |
| `priming-policy.json` | Cold-start source order and missing-source behavior. |
| `persistence-policy.json` | Allowed persistence targets and authorization requirements. |
| `source-boundary-policy.json` | Approved local source paths and prohibited inference sources. |
