# katas-confidence-stratified-sampling Scripts

`check.sh` runs deterministic offline fixtures through
`validate_confidence_report.py`.

The validator checks labeled validation, calibration buckets, stratified
sampling, segmented accuracy, calibrated routing, evidence, and validation
flags. It does not call the network or use random inputs.
