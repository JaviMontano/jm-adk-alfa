# Scripts

`validate_posttooluse_normalization.py` validates structured JSON reports for deterministic `PostToolUse` output normalization. `check.sh` runs valid fixtures and invalid mutations offline.
