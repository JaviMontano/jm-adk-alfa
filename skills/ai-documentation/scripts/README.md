# AI Documentation Scripts

Run:

```bash
bash skills/ai-documentation/scripts/check.sh
```

The check validates deterministic documentation packet JSON fixtures against
the local assets contract, source policy, doc type policy, gap policy, and safe
output path policy.
