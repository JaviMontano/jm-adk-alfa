# Scripts

`compile-agentic-loop.py` validates a structured agentic-loop contract and renders a deterministic Python control-loop skeleton.

Run the local gate:

```bash
bash skills/agentic-loop-engineering/scripts/check.sh
```

The gate compiles valid fixtures, checks required code fragments, and confirms invalid anti-pattern fixtures fail.
