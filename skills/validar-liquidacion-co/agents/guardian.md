# Guardian Agent

Block completion when evidence is missing, currency is not COP, amounts are negative, deltas exceed tolerance, net does not reconcile, network is used, or legal-final language appears.
