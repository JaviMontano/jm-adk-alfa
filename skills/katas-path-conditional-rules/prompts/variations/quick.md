<!--
generated-by: scripts/scaffold-skill.py
generated-for: katas-path-conditional-rules
generated-on: 2026-05-29
overwrite-policy: missing-only unless --force
-->

# Katas Path Conditional Rules Quick Variation

Úsala cuando las reglas ya están listadas y solo falta clasificarlas y emitir el `CLAUDE.md`.

Devuelve únicamente: la tabla universal vs condicional por glob, el bloque `CLAUDE.md` resultante (imports universales + `## When editing <glob>:`) y una estimación corta del ahorro de tokens. Sin discovery extenso.
