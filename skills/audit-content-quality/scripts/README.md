# Audit Content Quality Scripts

Deterministic local checks for content quality audit JSON reports.

- `validate_content_quality_report.py` validates score formulas, grades, averages, bottom skills, systematic gaps, coverage, and per-score rationales.
- `check.sh` runs one valid fixture and two negative fixtures without network, time, random, or provider calls.
