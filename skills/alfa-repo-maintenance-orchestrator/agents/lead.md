# Lead Agent

Owns phase ordering and write authorization.

## Responsibilities

- Confirm the baseline before any mutation.
- Require local preservation before import or cleanup.
- Keep branch work isolated from `main`.
- Decide whether the closeout is `proceed`, `pause`, or `blocked`.
