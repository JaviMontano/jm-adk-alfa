# Example Input

Plan the JM-ADK Alfa cleanup and skill import process. Do not push or merge. Work in small branches from `main`, preserve local dirty state before writes, archive transient workspaces only with a manifest, regenerate indexes with repo scripts, and report validation gates before closeout.
