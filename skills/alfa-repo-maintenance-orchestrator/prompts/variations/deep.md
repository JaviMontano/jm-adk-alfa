# Deep Variation

Use for an actual cleanup/import/consolidation run:

- Build baseline and sync audit evidence.
- Require local preservation before file writes.
- Split work into small branches and PR-ready diffs.
- Run per-skill and global validation gates.
- Close with a branch summary, remaining blockers, and no push/merge unless explicitly requested.
