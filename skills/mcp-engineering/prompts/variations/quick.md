# Mcp Engineering Quick Variation

Úsala cuando el servidor y el scope ya están claros y solo falta materializar la config.

Entrega solo: el bloque `.mcp.json` (o `~/.claude.json`) con credenciales por `${ENV}`, el contrato de error tipado, retry policy del cliente, estado del checklist y riesgos residuales. Sin discusión de diseño.
