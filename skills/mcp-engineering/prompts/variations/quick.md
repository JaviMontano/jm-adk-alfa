<!--
generated-by: scripts/scaffold-skill.py
generated-for: mcp-engineering
generated-on: 2026-05-30
overwrite-policy: missing-only unless --force
-->

# Mcp Engineering Quick Variation

Úsala cuando el servidor y el scope ya están claros y solo falta materializar la config.

Entrega solo: el bloque `.mcp.json` (o `~/.claude.json`) con credenciales por `${ENV}`, el contrato de error tipado, el estado del checklist y los riesgos residuales. Sin discusión de diseño.
