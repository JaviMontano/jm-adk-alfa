# Scripts — mcp-engineering

`validate_mcp_engineering.py` validates a JSON MCP engineering report against the deterministic assets for this skill.

`check.sh` runs the validator against valid fixtures and invalid mutations. It is offline, deterministic, and does not require network access.
