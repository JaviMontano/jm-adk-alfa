# Example Input

Antes de cambiar nuestro flujo de PR, verifica en fuentes oficiales si `gh pr merge --squash --delete-branch` puede borrar la rama remota tras merge.

Hay un blog que lo afirma, pero quiero que la decisión de actualizar el runbook dependa de documentación oficial. Entrega un reporte JSON con fuentes, fecha de consulta, claims, decisión y riesgos.
