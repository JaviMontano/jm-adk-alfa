# Scripts — official-source-verifier

`validate_official_source_verifier.py` validates a JSON official-source verification report against the deterministic assets for this skill.

`check.sh` runs the validator against valid fixtures and invalid mutations. It is offline, deterministic, and does not require network access.
